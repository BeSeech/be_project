import {TaskModel} from '../../data/model/task/task';

export class TaskPositionApi {
  public postPosition(taskUid: string, newHostUid: string, newPositoinIndex: number): void {
    // The code below have to be deleted. cos it's a server logic
  }
}
