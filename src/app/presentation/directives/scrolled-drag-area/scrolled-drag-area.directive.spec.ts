import { ScrolledDragAreaDirective } from './scrolled-drag-area.directive';

describe('ScrolledDragAreaDirective', () => {
  it('should create an instance', () => {
    const directive = new ScrolledDragAreaDirective();
    expect(directive).toBeTruthy();
  });
});
